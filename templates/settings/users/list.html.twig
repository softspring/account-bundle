{% trans_default_domain 'sfs_account' %}
{% extends '@SfsAccount/settings_layout.html.twig' %}

{% block content %}

    {% embed '@SfsComponents/paginator/table.html.twig' with {'collection':memberships} %}
        {% trans_default_domain 'sfs_account' %}
        {% block ths %}
            <th scope="col">{{ 'settings_users.list.fields.id.header'|trans }}</th>
{#            <th>&nbsp;</th>#}
        {% endblock ths %}

        {% block tbody %}
            {% for membership in memberships %}
                <tr>
                    <td>
                        {{ membership.user.username }}
                        {% if membership.user == app.user %}(me){% endif %}
                        {% if membership.user == account.owner %}(owner){% endif %}
                    </td>
                </tr>
            {% endfor %}
        {% endblock tbody %}
    {% endembed %}
{% endblock content %}
